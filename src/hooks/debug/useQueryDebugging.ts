
import { QueryClient } from '@tanstack/react-query';

/**
 * Simplified hook for debugging React Query
 */
export function useQueryDebugging({ queryClient }: { queryClient: QueryClient }) {
  // Simplified implementation
  return { queryClient };
}
